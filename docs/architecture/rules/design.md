# 设计规则

## 1. 默认规则

1. 设计应服务目标用户和核心任务，不用装饰替代信息架构。
2. 页面、组件、状态和交互应形成稳定系统，不为单次页面复制孤立样式。
3. UI 文案应表达用户可感知结果，不解释内部实现。
4. 响应式、可访问性、空态、加载态、错误态和权限不足态应进入验收。
5. 多语言界面必须按当前界面语言显示语言名称和用户任务，不向最终用户暴露 locale code；最长翻译、换行、按钮稳定尺寸和 RTL 适用性必须进入视觉验收。

## 2. 待项目补齐

1. 品牌基调。
2. 色彩、排版、间距和组件规范。
3. 关键页面截图验收标准。
4. 目标设备和断点。
